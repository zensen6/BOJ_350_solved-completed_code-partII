#include<bits/stdc++.h>
using namespace std;

typedef long long ll;
ll n,ans=0;
vector<ll> v;
stack<ll> st;
int main(){

    cin>>n;
    v = vector<ll> (n,0);
    for(ll i=0;i<n;i++){
      cin>>v[i];
    }

  return 0;
}
